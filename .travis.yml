# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
warnings_are_errors: false
sudo: false

cache:
  - packages
  - pip

addons:
  apt:
    packages:
      - python3-pip

before_install:
  - pip3 install c-lasso
