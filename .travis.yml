# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
python: 3.6
cache: packages
dist: bionic
warnings_are_errors: false

before_install:
#  - pip install --upgrade pip
  - pip install c-lasso

r_github_packages: jacobbien/ggb
